import Coordinate from "./Coordinate"
import Drawable from "../interface/Drawable";
import Dragable from "../interface/Dragable";

export default class Handle extends Coordinate implements Drawable, Dragable {
    onDrag () {

    }
    draw () {
        
    }
}